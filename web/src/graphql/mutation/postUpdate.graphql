mutation UpdatePost(
    $updatePostId: Int!,
    $title: String!,
    $text: String!
    ) {
    updatePost(
        id: $updatePostId,
        title: $title,
        text: $text
        ) {
            id
            title
            text
        }
}