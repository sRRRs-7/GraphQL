query Post($postId: Int!) {
    post(id: $postId) {
        id
        title
        textSnippet
        text
        points
        creatorId
        creator {
            id
            username
        }
    }
}